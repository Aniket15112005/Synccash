diff --git a/lib/features/sales/presentation/screens/native_pdf_viewer_native.dart b/lib/features/sales/presentation/screens/native_pdf_viewer_native.dart
index 1538bef..09deb5c 100644
--- a/lib/features/sales/presentation/screens/native_pdf_viewer_native.dart
+++ b/lib/features/sales/presentation/screens/native_pdf_viewer_native.dart
@@ -106,7 +106,6 @@ class _NativePdfViewerPageState extends State<_NativePdfViewerPage> {
       final safeName = widget.billNumber.replaceAll(RegExp(r'[^\w\-]'), '_');
       await Share.shareXFiles(
         [XFile(path, name: 'Invoice_$safeName.pdf', mimeType: 'application/pdf')],
-        subject: 'Invoice #${widget.billNumber} — ${widget.partyName}',
       );
     } catch (e) {
       if (mounted) {
diff --git a/lib/features/sales/presentation/screens/web_invoice_viewer_web.dart b/lib/features/sales/presentation/screens/web_invoice_viewer_web.dart
index 2bb421d..50b1b71 100644
--- a/lib/features/sales/presentation/screens/web_invoice_viewer_web.dart
+++ b/lib/features/sales/presentation/screens/web_invoice_viewer_web.dart
@@ -12,6 +12,7 @@ import 'dart:async';
 import 'dart:html' as html;
 import 'dart:ui_web' as ui_web;
 import 'package:flutter/material.dart';
+import 'package:http/http.dart' as http;
 import 'package:share_plus/share_plus.dart';
 
 void openPdfInApp(
@@ -51,6 +52,7 @@ class _WebInvoiceViewerPageState extends State<_WebInvoiceViewerPage> {
 
   bool _loading = true;
   bool _errored = false;
+  bool _sharing = false;
   Timer? _loadingFallbackTimer;
 
   @override
@@ -108,24 +110,36 @@ class _WebInvoiceViewerPageState extends State<_WebInvoiceViewerPage> {
     if (mounted) setState(() => _loading = false);
   }
 
-  // Shares the invoice link directly (Share.share) rather than fetching the
-  // PDF bytes first. Fetching bytes on web would need a CORS-enabled GET to
-  // the storage host and can noticeably lag on larger PDFs / slow networks;
-  // sharing the URL is instant, works with the OS/browser share sheet on
-  // every platform (mobile web + desktop web), and needs no extra network
-  // round trip beyond what's already happening in the iframe.
+  // Shares the actual PDF file (not a link). We fetch the bytes once, then
+  // hand them to Share.shareXFiles so the OS/browser share sheet attaches
+  // the real .pdf — no subject/description text is passed, so recipients
+  // just get the file itself, exactly like sharing a normal PDF.
   Future<void> _sharePdf() async {
+    if (_sharing) return;
+    setState(() => _sharing = true);
     try {
-      await Share.share(
-        widget.url,
-        subject: 'Invoice #${widget.billNumber} — ${widget.partyName}',
-      );
+      final res = await http
+          .get(Uri.parse(widget.url))
+          .timeout(const Duration(seconds: 30));
+      if (res.statusCode != 200) {
+        throw Exception('HTTP ${res.statusCode}');
+      }
+      final safeName = widget.billNumber.replaceAll(RegExp(r'[^\w\-]'), '_');
+      await Share.shareXFiles([
+        XFile.fromData(
+          res.bodyBytes,
+          name: 'Invoice_$safeName.pdf',
+          mimeType: 'application/pdf',
+        ),
+      ]);
     } catch (e) {
       if (mounted) {
         ScaffoldMessenger.of(context).showSnackBar(
           SnackBar(content: Text('Could not share: $e')),
         );
       }
+    } finally {
+      if (mounted) setState(() => _sharing = false);
     }
   }
 
@@ -164,8 +178,15 @@ class _WebInvoiceViewerPageState extends State<_WebInvoiceViewerPage> {
         ),
         actions: [
           IconButton(
-            onPressed: _sharePdf,
-            icon: const Icon(Icons.share_rounded, color: Colors.white70),
+            onPressed: _sharing ? null : _sharePdf,
+            icon: _sharing
+                ? const SizedBox(
+                    width: 18,
+                    height: 18,
+                    child: CircularProgressIndicator(
+                        color: Colors.white70, strokeWidth: 2),
+                  )
+                : const Icon(Icons.share_rounded, color: Colors.white70),
             tooltip: 'Share',
           ),
         ],
