// Stub for web platform — openNativePdfInApp is a no-op here because web
// always takes the kIsWeb branch and never reaches native code.

import 'package:flutter/material.dart';

void openNativePdfInApp(
  BuildContext context,
  String url,
  String billNumber,
  String partyName,
) {}
